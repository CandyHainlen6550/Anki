# Generated package

`dist/HT Joyo 2136.apkg` and its QA files are generated by GitHub Actions from the committed split sources.

The repository ZIP intentionally does not ship a stale APKG after source refactors. Push the repository and the build workflow will regenerate and verify the permanent package name `HT Joyo 2136.apkg`.
